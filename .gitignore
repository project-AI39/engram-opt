# build/ はセットアップスクリプトが生成するステージング領域（配布Zipと同一構造）
build/

# パッケージャー（go run ./cmd/engram-package）の出力先
/dist/

# go build ./... の生成バイナリがリポジトリ直下に出ることがあるため除外
/*.exe


# 実動画テスト用（ローカル限定）
/test.webm

# 実動画テストの生成物
/*.opt.mkv
/testrun.log

# 手元生成のテスト動画フィクスチャ（Gitコミット禁止方針）
/testdata/
